# 服务名
spring:
  application:
    name: beacon-test
  # nacos地址
  cloud:
    nacos:
      discovery:
        #server-addr: 192.168.31.121:8848
        server-addr: 192.168.49.36:8848
  # datasource
  datasource:
    driver-class-name: org.gjt.mm.mysql.Driver
    url: jdbc:mysql://192.168.49.36:3306/beacon_cloud?characterEncoding=utf-8&useSSL=false
    username: root
    password: 'xxiao0123'
# 端口号
server:
  port: 20000

# mybatis
mybatis:
  configuration:
    map-underscore-to-camel-case: true


# 在调用方模块的配置文件中添加
feign:
  client:
    config:
      default:  # 全局默认配置
        connectTimeout: 50000   # 连接超时 50 秒
        readTimeout: 300000     # 读取超时 300 秒

      # 针对 beacon-cache 服务的特殊配置（会覆盖全局配置）
      beacon-cache:
        readTimeout: 600000     # 单独设置此服务的读取超时为 600 秒